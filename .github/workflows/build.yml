name: "Build"

on:
  - "push"
  - "workflow_dispatch"

concurrency:
  group: "build-${{ github.ref }}"
  cancel-in-progress: true

permissions:
  contents: "read"
  packages: "write"

jobs:
  set-matrix:
    name: "⊹"
    runs-on: "ubuntu-latest"
    outputs:
      matrix: "${{ steps.set-matrix.outputs.matrix }}"
    steps:
      - uses: "actions/checkout@v3"

      - uses: "ruby/setup-ruby@v1"
        with:
          ruby-version: "3.1"

      - id: "set-matrix"
        run: rake github:actions:matrix

  build-rubies:
    name: "${{ matrix.short-slug }}"
    needs: "set-matrix"
    runs-on: "ubuntu-latest"
    strategy:
      fail-fast: false
      matrix: "${{ fromJson(needs.set-matrix.outputs.matrix) }}"
    steps:
      - uses: "actions/checkout@v3"

      - uses: "ruby/setup-ruby@v1"
        with:
          ruby-version: "3.1"

      - name: Setup utils
        run: |
          echo "$PWD/.github/workflows/bin" >> $GITHUB_PATH

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # We use qemu so Ruby can be built without a baseruby, we make sure to
      # uninstall it before testing so we can assert things work without it.
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2
        with:
          platforms: ${{ join(matrix.docker.platforms, ',') }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Docker meta
        uses: docker/metadata-action@v4
        id: meta
        with:
          images: ${{ matrix.docker.repo }}
          tags: type=sha

      - name: Format args
        uses: actions/github-script@v6
        id: format
        with:
          script: |
            const serialize = (val) => Object.entries(val).map(([key, value]) => `${key}=${value}`);

            const labels = ${{ matrix.docker.labels }};
            core.setOutput('labels', serialize(labels));

            const buildArgs = ${{ matrix.docker.build-args }};
            core.setOutput('build-args', serialize(buildArgs));

      - name: Build and push
        uses: docker/build-push-action@v3
        with:
          context: .
          push: true
          file: ${{ matrix.docker.file }}
          platforms: ${{ join(matrix.docker.platforms, ',') }}
          build-args: |
            ${{ join(fromJson(steps.format.outputs.build-args), '\n') }}
          tags: |
            ${{ join(matrix.docker.tags, '\n') }}
          labels: |
            ${{ join(fromJson(steps.format.outputs.labels), '\n') }}
            ${{ steps.meta.outputs.labels }}

      - name: "Test"
        env:
          DOCKER_TAGS: "${{ toJson(matrix.docker.tags) }}"
        run: |
          : Testing image
          echo "::info::Disabling binfmt_misc for testing"
          docker run --privileged --rm tonistiigi/binfmt --uninstall qemu-*
          each-docker-tag.sh -- bin/test --image %DOCKER_TAG%
